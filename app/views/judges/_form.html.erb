<%= form_for(judge) do |f| %>
  <% if judge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(judge.errors.count, "error") %> prohibited this judge from being saved:</h2>

      <ul>
      <% judge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :passsword %>
    <%= f.number_field :passsword %>
  </div>

  <div class="field">
    <%= f.label :admin_user %>
    <%= f.number_field :admin_user %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
